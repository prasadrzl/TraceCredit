{
  "name": "tracecredit",
  "private": true,
  "workspaces": [
    "api",
    "web"
  ],
  "scripts": {
    "dev:web": "yarn workspace tracecredit-web dev",
    "dev:api": "yarn workspace tracecredit-api start:dev",
    "build": "yarn workspaces run build",
    "test:contracts": "cd contracts && forge test",
    "test:api": "yarn workspace tracecredit-api test",
    "lint": "yarn workspaces run lint"
  }
}

{
  "name": "tracecredit-subgraph",
  "version": "1.0.0",
  "scripts": {
    "codegen": "graph codegen",
    "build":   "graph build",
    "deploy":  "graph deploy --node https://api.studio.thegraph.com/deploy/ tracecredit",
    "deploy:local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 tracecredit"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.73.0",
    "@graphprotocol/graph-ts":  "^0.35.1"
  }
}
